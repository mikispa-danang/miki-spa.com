# Miki Skin Spa — Hướng dẫn quản trị nội dung

1. Mở toàn bộ thư mục website bằng VS Code.
2. Cài extension **Live Server** và mở `index.html` bằng Live Server.
3. Mở `admin.html` bằng trình duyệt, tốt nhất Chrome hoặc Edge.
4. Chỉnh nội dung, giá hoặc đường dẫn hình ảnh trong giao diện quản trị.
5. Bấm **Lưu & xem trước** để xem thay đổi trên chính máy của bạn.
6. Khi nội dung đã phù hợp, vào tab **Xuất bản**:
   - Cách dễ: bấm **Tải content-data.js** rồi chép đè file `content-data.js` trong thư mục website.
   - Hoặc Chrome/Edge: bấm **Chọn file và lưu**, chọn file `content-data.js` hiện có để ghi đè.
7. Tải lại hoặc triển khai lại thư mục website để nội dung mới hiển thị cho khách hàng.

Lưu ý: `admin.html` là công cụ quản trị cục bộ, không phải CMS có đăng nhập bảo mật. Không nên dùng nó như trang quản trị trực tuyến chứa dữ liệu nhạy cảm.
